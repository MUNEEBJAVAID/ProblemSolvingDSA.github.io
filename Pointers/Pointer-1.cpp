#include <iostream>
using namespace std;

int main() 
{
  /*
   int a = 6;
   int b = 6;
   
   cout << a << endl;
   
   cout << &a << endl;
   cout << &b << endl;
   
   int num = 6;
   
  // pointer create
  
  // ptr is a pointer to a in which contain int data.
  int* ptr = &num;

  // access the value ptr is pointing to
  // * is derefernced operator 
  
  cout << num << endl;
  cout <<"Address of num: " << &num << endl;
  cout <<"Address stored at ptr: " <<  ptr << endl;
  cout << "Vaue stored at ptr: " << *ptr << endl;
  cout <<"Address of ptr: " << &ptr << endl;
  
  // sizeof ptr
  
     int num = 6;
     int* numPtr = &num;
     
     char ch = 'c';
     char* chPtr = &ch;

     cout << sizeof(numPtr) << endl;
     cout << sizeof(chPtr) << endl;
     
  // Bad Practice
  int* ptr;
  
  cout << ptr << endl;
  cout << *ptr << endl;
  
  // how can we correct this?
  
  // we should use
  // nullptr , 0 , NULL
  
  int *ptr1 = 0;
  int *ptr2 = NULL;
  int *ptr3 = nullptr;
  
  
  cout << *ptr1 <<endl;
  cout << *ptr1 <<endl;
  cout << *ptr1 <<endl;
    
   int num = 9;
   
   int *ptr = &num;
   
  // copy pointer
   int *copyPtr = ptr;
   */
   
  
    
    
    
    
    
    
    
    
    
    
    
    
    return 0;
}
